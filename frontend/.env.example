# Leave empty in dev to use Vite's proxy (`/api` -> http://localhost:3000).
# In production set this to the absolute backend URL (e.g. https://api.example.com)
# and make sure the backend has CORS enabled.
VITE_API_BASE_URL=
